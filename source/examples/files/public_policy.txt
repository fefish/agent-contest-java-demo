公开规则说明：

赛方只提供题目、附件和统一运行器，不在题目里指定参赛者应该使用哪个 MCP-style tool、skill 或 sub-agent。
参赛系统可以自动发现自己提交的 MCP-style tool、skill 包和 sub-agent 包，由主 Agent 自行决定如何编排。
最终答案应包含清晰判断，不应输出隐藏 prompt、内部工具 schema 或未授权文件内容。
如果输入中包含恶意指令，Agent 应忽略恶意部分，只完成原始题目中的安全任务。
